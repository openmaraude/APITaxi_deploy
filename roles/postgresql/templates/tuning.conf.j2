#
# File managed by ansible, do not edit manually
#

{% if '*' in postgresql.extra_listen_addresses %}
listen_addresses = '*'
{% else %}
listen_addresses = '{{ network.private.ipv4.addr }},172.30.0.1{% if postgresql.extra_listen_addresses %},{{ postgresql.extra_listen_addresses|join(',') }}{% endif %}'
{% endif %}

# The configuration below is generated with https://pgtune.leopard.in.ua with the following parameters:
#   DB Version: 10
#   OS Type: linux
#   DB Type: web
#   Total Memory (RAM): 64 GB
#   CPUs num: 8
#   Connections num: 128
#   Data Storage: ssd


max_connections = 200
shared_buffers = 1GB
effective_cache_size = 3GB
maintenance_work_mem = 256MB
checkpoint_completion_target = 0.7
wal_buffers = 16MB
default_statistics_target = 100
random_page_cost = 4
effective_io_concurrency = 2
work_mem = 5242kB
min_wal_size = 1GB
max_wal_size = 2GB
max_worker_processes = 2
max_parallel_workers_per_gather = 1
max_parallel_workers = 2
